#ifndef MATRIS_C
#define MATRIS_C
float ** createMatrix(int);
void printMatris(float **,int n);
void fillArray(float **,int );
float **multiply(float **, float **,int );
float **multiplyikj(float **, float **,int );
float **multiplykji(float **, float **,int );
float **multiplyjik(float **, float **,int );
float **multiplyjki(float **, float **,int );
float **multiplykij(float **, float **,int );
#endif // MATRIS_C
